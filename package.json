{
  "name": "calendar-sync",
  "version": "1.0.0",
  "description": "Google Apps Script project synced with GitHub CI/CD",
  "scripts": {
    "login": "clasp login",
    "logout": "clasp logout",
    "pull": "clasp pull",
    "push": "clasp push",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
